*! Date     : 2017-10-24
*! version  : 0.1
*! Author   : Richard Herron
*! Email    : richard.c.herron@gmail.com

*! install packages in new install

/* 
2017-10-24 v0.1 
*/

program define new_install

    capture ssc install carryforward
    capture ssc install cem
    capture ssc install dropmiss
    capture ssc install estout
    capture ssc install ftools
    capture ssc install freduse
    capture ssc install ivreg2
    capture ssc install nearmrg
    capture ssc install psmatch2
    capture ssc install ranktest
    capture ssc install reghdfe
    capture ssc install revrs
    capture ssc install save13
    capture ssc install shufflevar
    capture ssc install tsegen
    capture ssc install unique
    capture ssc install winsor2
    capture ssc install xtfmb
    capture ssc install xtivreg2
    capture ssc install xtoverid    

    end


